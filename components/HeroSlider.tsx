'use client';
import Image from 'next/image';import Link from 'next/link';import{ArrowLeft,ChevronLeft,ChevronRight,Pause,Play}from'lucide-react';import{useEffect,useMemo,useState}from'react';
type HeroSlide={id:string;image:string;title:string;title_en?:string;subtitle:string;subtitle_en?:string;body:string;body_en?:string;button_text:string;button_text_en?:string;button_url:string;visible?:boolean};
type HeroData={slides?:HeroSlide[];interval?:number};
function readHeroData(raw:unknown):HeroData{try{const parsed=JSON.parse(typeof raw==='string'?raw:'{}');return parsed&&typeof parsed==='object'?parsed:{}}catch{return {}}}
export default function HeroSlider({hero,en}:{hero:any;en:boolean}){const data=useMemo(()=>readHeroData(hero?.data),[hero?.data]);const slides=useMemo(()=>{const configured=Array.isArray(data.slides)?data.slides.filter((slide:HeroSlide)=>slide&&slide.visible!==false):[];if(configured.length)return configured;return[{id:'default',image:hero?.image||'',title:hero?.title||(en?'We turn ideas into powerful digital systems.':'نحوّل أفكارك إلى أنظمة رقمية قوية.'),title_en:hero?.title_en,subtitle:hero?.subtitle||(en?'Built around your work.':'تشتغل معاك.'),subtitle_en:hero?.subtitle_en,body:hero?.body||(en?'We design and build modern websites, custom business systems and digital experiences that help ambitious businesses move faster.':'من موقع احترافي إلى نظام متكامل لإدارة شغلك، بنحوّل احتياجاتك إلى تجربة رقمية واضحة، سريعة، ومصممة حول طريقة عملك.'),body_en:hero?.body_en,button_text:hero?.button_text||'ابدأ مشروعك',button_text_en:hero?.button_text_en||'Start your project',button_url:hero?.button_url||'#contact',visible:true}]},[data.slides,hero,en]);const[active,setActive]=useState(0);const[paused,setPaused]=useState(false);useEffect(()=>setActive(0),[slides.length]);const interval=Math.min(30000,Math.max(2000,Number(data.interval)*1000||6000));useEffect(()=>{if(slides.length<2||paused)return;const timer=window.setInterval(()=>setActive(current=>(current+1)%slides.length),interval);return()=>window.clearInterval(timer)},[slides.length,paused,interval]);const slide=slides[active]||slides[0];const title=en?slide.title_en||slide.title:slide.title;const subtitle=en?slide.subtitle_en||slide.subtitle:slide.subtitle;const body=en?slide.body_en||slide.body:slide.body;const buttonText=en?slide.button_text_en||slide.button_text:slide.button_text;const move=(direction:number)=>setActive(current=>(current+direction+slides.length)%slides.length);return <section className="hero-shell"><div className="hero container" aria-label={en?'Hero slider':'الواجهة الرئيسية'}>{slide.image&&<div className="hero-background" aria-hidden="true"><Image src={slide.image} alt="" fill priority={active===0} sizes="100vw"/></div>}<div className="hero-overlay" aria-hidden="true"/><div className="hero-content"><div className="hero-pill">ShoTech Solutions</div><h1>{title}{subtitle&&<span>{subtitle}</span>}</h1>{body&&<p className="hero-lead">{body}</p>}<div className="hero-actions">{buttonText&&slide.button_url&&<Link href={slide.button_url} className="btn btn-primary">{buttonText}<ArrowLeft size={18}/></Link>}<Link href="#projects" className="btn btn-outline">{en?'View our work':'شوف أعمالنا'}</Link></div><div className="hero-note"><span><i/>{en?'Custom solutions':'حلول مخصصة'}</span><span><i/>{en?'Design & UX':'تصميم وتجربة مستخدم'}</span><span><i/>{en?'Ongoing support':'دعم وتطوير مستمر'}</span></div></div>{slides.length>1&&<div className="hero-controls" aria-label={en?'Hero controls':'التحكم في الواجهة'}><button type="button" onClick={()=>move(-1)} aria-label={en?'Previous slide':'الشريحة السابقة'}><ChevronRight size={18}/></button><button type="button" onClick={()=>setPaused(value=>!value)} aria-label={paused?(en?'Play slider':'تشغيل العرض'):(en?'Pause slider':'إيقاف العرض')}>{paused?<Play size={15}/>:<Pause size={15}/>}</button><button type="button" onClick={()=>move(1)} aria-label={en?'Next slide':'الشريحة التالية'}><ChevronLeft size={18}/></button><div className="hero-dots">{slides.map((item,index)=><button type="button" key={item.id||index} className={index===active?'active':''} onClick={()=>setActive(index)} aria-label={`${en?'Slide':'الشريحة'} ${index+1}`}/>)}</div></div>}</div><style jsx>{`.hero-shell{position:relative;overflow:hidden;border-bottom:1px solid rgba(134,155,220,.1)}.hero{position:relative;min-height:650px;display:flex;align-items:center;overflow:hidden;border-radius:0}.hero-background{position:absolute;inset:0}.hero-background img{object-fit:cover;object-position:center}.hero-overlay{position:absolute;inset:0;background:linear-gradient(90deg,rgba(5,8,22,.97) 0%,rgba(5,8,22,.82) 48%,rgba(5,8,22,.45) 100%)}.hero-content{position:relative;z-index:2;width:min(760px,100%);padding:90px 0}.hero-pill{display:inline-flex;padding:8px 13px;border:1px solid rgba(81,176,255,.3);background:rgba(35,91,175,.13);border-radius:999px;color:#b9e4ff;font-size:12px;font-weight:800}.hero h1{font-size:clamp(42px,6vw,76px);line-height:1.08;letter-spacing:-.055em;margin:20px 0}.hero h1 span{display:block;color:#36c4ff}.hero-lead{max-width:680px;color:#98a6c5;line-height:1.85;font-size:17px}.hero-actions{display:flex;flex-wrap:wrap;gap:12px;margin-top:28px}.btn-primary{background:linear-gradient(105deg,#1773ff,#36c4ff,#8b5cf6)}.btn-outline{background:rgba(255,255,255,.025);border:1px solid rgba(133,155,225,.22)}.hero-note{display:flex;flex-wrap:wrap;gap:18px;margin-top:26px;color:#a7b5d4;font-size:13px}.hero-note span{display:flex;align-items:center;gap:7px}.hero-note i{width:7px;height:7px;border-radius:50%;background:#36c4ff;box-shadow:0 0 10px rgba(54,196,255,.7)}.hero-controls{position:absolute;z-index:3;right:4%;bottom:34px;display:flex;align-items:center;gap:8px}.hero-controls>button{width:40px;height:40px;border-radius:11px;border:1px solid rgba(133,155,225,.24);background:rgba(5,8,22,.72);color:#fff;display:grid;place-items:center;cursor:pointer}.hero-dots{display:flex;gap:6px;margin-inline:6px}.hero-dots button{width:24px;height:4px;padding:0;border:0;border-radius:9px;background:rgba(255,255,255,.3);cursor:pointer}.hero-dots button.active{width:38px;background:#36c4ff}@media(max-width:700px){.hero{min-height:650px;width:100%}.hero-overlay{background:linear-gradient(180deg,rgba(5,8,22,.76),rgba(5,8,22,.96) 76%)}.hero-content{padding:80px 4% 110px}.hero h1{font-size:clamp(36px,10vw,52px);margin:18px 0}.hero-lead{font-size:15px}.hero-note{gap:10px;font-size:11px}.hero-controls{right:4%;bottom:24px}.hero-actions .btn{width:100%}}`}</style></section>}
