'use client';
import Link from 'next/link';import Image from 'next/image';import {usePathname} from 'next/navigation';import {Menu,X,ArrowLeft} from 'lucide-react';import {useState} from 'react';import LanguageSwitcher from './LanguageSwitcher';import type {Locale} from '@/lib/i18n';import {t} from '@/lib/i18n';
export default function Header({locale}:{locale:Locale}){const pathname=usePathname();const [open,setOpen]=useState(false);if(pathname.startsWith('/admin'))return null;const links=[[t(locale,'home'),'/'],[t(locale,'services'),'/#services'],[t(locale,'projects'),'/#projects'],[t(locale,'about'),'/#about'],[t(locale,'contact'),'/#contact']];return <header className="site-header"><div className="container nav"><Link href="/" className="brand" onClick={()=>setOpen(false)} aria-label="ShoTech"><Image src="/shotech-header.png" alt="ShoTech" width={220} height={74} priority/></Link><nav className={`nav-links ${open?'is-open':''}`}>{links.map(([label,href])=><Link key={href} href={href} className={pathname===href?'active':''} onClick={()=>setOpen(false)}>{label}</Link>)}</nav><div className="nav-actions"><LanguageSwitcher locale={locale}/><Link href="/#contact" className="btn btn-primary nav-cta">{t(locale,'start')} <ArrowLeft size={17}/></Link><button className="menu-toggle" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X size={23}/>:<Menu size={23}/>}</button></div></div></header>}
