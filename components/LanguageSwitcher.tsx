'use client';import{usePathname,useRouter}from'next/navigation';import{useEffect,useState}from'react';const COOKIE='shotech-locale';export default function LanguageSwitcher({locale}:{locale:'ar'|'en'}){const router=useRouter();const pathname=usePathname();const[busy,setBusy]=useState(false);useEffect(()=>setBusy(false),[pathname]);function change(next:'ar'|'en'){if(next===locale||busy)return;setBusy(true);document.cookie=`${COOKIE}=${next}; path=/; max-age=31536000; samesite=lax`;router.refresh()}return <button type="button" className="language-switcher" onClick={()=>change(locale==='ar'?'en':'ar')} disabled={busy} aria-label={locale==='ar'?'Switch to English':'التبديل إلى العربية'}>{locale==='ar'?'EN':'عربي'}</button>}
