'use client';import{useState}from'react';import{ArrowLeft,Mail,MapPin,MessageCircle,Phone,Clock}from'lucide-react';import type{Locale}from'@/lib/i18n';
export default function HomeContact({settings,locale}:{settings:any;locale:Locale}){const en=locale==='en',email=settings.email||'hello@shotech.dev',phone=settings.phone||settings.whatsapp||'',whatsapp=settings.whatsapp||phone,address=en?(settings.address_en||settings.address||'Egypt · Serving clients wherever they are'):(settings.address||'مصر · نخدم عملاءنا أينما كانوا'),hours=en?(settings.working_hours_en||settings.working_hours||'Saturday – Thursday · 9 AM – 5 PM'):(settings.working_hours||'السبت – الخميس · 9 ص – 5 م');const[ok,setOk]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);try{const form=e.currentTarget,r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});setOk(r.ok?(en?'Your message has been received. We will get back to you soon.':'تم استلام رسالتك، وهنرجعلك قريب.'):(en?'Something went wrong. Please try again.':'حصلت مشكلة، جرّب مرة تانية.'));if(r.ok)form.reset()}catch{setOk(en?'Something went wrong. Please try again.':'حصلت مشكلة، جرّب مرة تانية.')}finally{setLoading(false)}}return <><section id="contact" className="section section-soft home-contact"><div className="container"><div className="section-head"><div><div className="eyebrow">{en?'Contact us':'تواصل معنا'}</div><h2>{en?'Tell us about your project.':'احكيلنا عن مشروعك.'}</h2></div><p className="muted">{en?'Start with a simple idea and we will help shape it into a clear next step.':'ابدأ بفكرة بسيطة وإحنا نساعدك نحولها لخطوة واضحة.'}</p></div><div className="contact-layout"><div className="form-card"><form className="form" onSubmit={submit}><div className="form-row"><input className="input" name="name" placeholder={en?'Name':'الاسم'} required/><input className="input" name="email" type="email" placeholder="Email" required/></div><div className="form-row"><input className="input" name="phone" placeholder={en?'Phone / WhatsApp':'رقم الهاتف / واتساب'}/><input className="input" name="company" placeholder={en?'Company (optional)':'اسم الشركة (اختياري)'}/></div><input className="input" name="subject" placeholder={en?'Project or service':'نوع المشروع أو الخدمة'}/><textarea name="message" placeholder={en?'Tell us about your idea or the problem you need to solve...':'احكيلنا عن فكرتك أو المشكلة اللي محتاج تحلها...'} required/><button className="btn btn-primary" type="submit" disabled={loading}>{loading?(en?'Sending...':'جاري الإرسال...'):(en?'Send request':'إرسال الطلب')} <ArrowLeft size={17}/></button>{ok&&<p className="muted">{ok}</p>}</form></div><div className="home-contact-copy"><h3>{en?'Let’s start from the problem.':'خلينا نبدأ من المشكلة.'}</h3><p className="muted">{en?'Share a few details and we will get back to you with the right next step.':'ابعتلنا تفاصيل بسيطة، وهنرجعلك بالخطوة المناسبة.'}</p></div></div></div></section><section id="call-us" className="section home-contact-info"><div className="container"><div className="section-head"><div><div className="eyebrow">{en?'Get in touch':'اتصل بينا'}</div><h2>{en?'Contact details.':'بيانات التواصل.'}</h2></div></div><div className="contact-info-grid"><div className="contact-item"><Mail/><div><b>Email</b><span>{email}</span></div></div><div className="contact-item"><Phone/><div><b>{en?'Phone':'رقم الهاتف'}</b><span>{phone}</span></div></div><div className="contact-item"><MessageCircle/><div><b>WhatsApp</b><span>{whatsapp}</span></div></div><div className="contact-item"><MapPin/><div><b>{en?'Address':'العنوان'}</b><span>{address}</span></div></div><div className="contact-item"><Clock/><div><b>{en?'Working hours':'مواعيد العمل'}</b><span>{hours}</span></div></div></div></div></section></>}