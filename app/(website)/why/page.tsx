import {getSections} from '@/lib/site';import {Sparkles,ShieldCheck,Rocket} from 'lucide-react';
export const dynamic='force-dynamic';const icons:any={Sparkles,ShieldCheck,Rocket};
export default async function Page(){const rows=await getSections('why');const s:any=rows[0]||{title:'Why ShoTech',subtitle:'WHY CHOOSE US',body:'Custom-built digital solutions focused on real business results.',data:'{}'};let items:any[]=[];try{items=JSON.parse(s.data||'{}').items||[]}catch{}if(!items.length)items=[['Sparkles','Custom-built','No one-size-fits-all solutions.'],['ShieldCheck','Reliable','A strong foundation designed to grow.'],['Rocket','Fast delivery','Focused execution from idea to launch.']];return <main className="container page"><div className="eyebrow">{s.subtitle}</div><h1>{s.title}</h1><p className="lead muted">{s.body}</p><div className="grid">{items.map((x:any,i:number)=>{const I=icons[x[0]]||Sparkles;return <article className="card" key={i}><div className="service-icon"><I size={26}/></div><h2>{x[1]}</h2><p className="muted">{x[2]}</p></article>})}</div></main>}
