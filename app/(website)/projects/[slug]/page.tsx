import{notFound}from'next/navigation';import Link from'next/link';import ProjectGallery from'./project-gallery';import{ArrowLeft}from'lucide-react';import{getProject}from'@/lib/site';import{getLocale}from'@/lib/i18n';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{slug:string}>}){const locale=await getLocale();const en=locale==='en';const{slug}=await params;const p:any=await getProject(slug,locale);if(!p)notFound();let tech:string[]=[],images:string[]=[];try{tech=JSON.parse(p.technologies||'[]')}catch{}try{images=JSON.parse(p.images||'[]')}catch{}if(p.cover_image&&!images.includes(p.cover_image))images.unshift(p.cover_image);images=images.filter(Boolean);return <main><section className="page-hero"><div className="container"><div className="eyebrow">{p.category||(en?'PROJECT':'مشروع رقمي')}</div><h1>{p.title}</h1>{p.excerpt&&<p>{p.excerpt}</p>}</div></section><section className="section"><div className="container"><div style={{marginBottom:40}}>{images.length?<ProjectGallery images={images} title={p.title}/>:<div className="detail-image"><div className="visual-center"><div className="visual-card"><b>{p.title}</b><p>{en?'Project details':'تفاصيل المشروع'}</p></div></div></div>}</div><div className="split" style={{alignItems:'start'}}><div><div className="eyebrow">{en?'About the project':'عن المشروع'}</div><h2>{en?'From problem to solution.':'من المشكلة للحل.'}</h2></div><div><p className="muted" style={{whiteSpace:'pre-wrap',fontSize:16}}>{p.content||p.excerpt}</p>{!!tech.length&&<div className="tags" style={{marginTop:25}}>{tech.map(x=><span className="tag" key={x}>{x}</span>)}</div>}{(p.demo_url||p.github_url)&&<div className="hero-actions">{p.demo_url&&<a className="btn btn-primary" href={p.demo_url} target="_blank" rel="noreferrer">{en?'View project':'مشاهدة المشروع'} <ArrowLeft size={16}/></a>}{p.github_url&&<a className="btn btn-outline" href={p.github_url} target="_blank" rel="noreferrer">GitHub <ArrowLeft size={16}/></a>}</div>}</div></div></div></section><section className="container section" style={{paddingTop:0}}><div className="cta"><div><div className="eyebrow">{en?'YOUR PROJECT':'مشروعك'}</div><h2>{en?'Want a similar project?':'عايز مشروع مشابه؟'}</h2><p>{en?'Tell us what you need and we will build the right solution.':'خلينا نعرف احتياجك ونبني الحل المناسب.'}</p></div><Link href="/#contact" className="btn btn-primary">{en?'Start a project':'ابدأ مشروعك'} <ArrowLeft size={17}/></Link></div></section></main>}
