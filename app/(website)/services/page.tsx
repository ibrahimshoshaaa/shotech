import Link from 'next/link';import {ArrowRight,Code2,ShoppingCart,Building2,LayoutDashboard,MonitorSmartphone,Workflow,Sparkles,Globe2,Database,Smartphone,Palette,ShieldCheck,Rocket,Settings,Layers3} from 'lucide-react';import {getServices} from '@/lib/site';
export const dynamic='force-dynamic';const icons:any={Code2,ShoppingCart,Building2,LayoutDashboard,MonitorSmartphone,Workflow,Sparkles,Globe2,Database,Smartphone,Palette,ShieldCheck,Rocket,Settings,Layers3};
export default async function Page(){const s=await getServices();return <main className="container page"><div className="eyebrow">SERVICES</div><h1>Built around your business.</h1><p className="lead muted">Explore every service and open its full detailed page.</p><div className="grid">{s.map((x:any)=>{const Icon=icons[x.icon]||Code2;return <article className="card" key={x.id}><div className="service-icon"><Icon size={25}/></div><h2>{x.title}</h2><p className="muted">{x.description}</p><Link className="card-link" href={`/services/${x.slug}`}>Explore service <ArrowRight size={16}/></Link></article>})}</div>{!s.length&&<div className="empty">The admin has not added services yet.</div>}</main>}
