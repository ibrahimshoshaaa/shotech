import { notFound } from 'next/navigation';
import Link from 'next/link';
import Image from 'next/image';
import { ArrowLeft } from 'lucide-react';
import { getService } from '@/lib/site';
import { getServiceIcon } from '@/lib/service-icons';
export const dynamic='force-dynamic';
function contentParagraphs(content:string,fallback:string){return (content||fallback||'').trim().split(/\n\s*\n|\n/).map(x=>x.trim()).filter(Boolean)}
export default async function ServiceDetailPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=await getService(slug);if(!service)notFound();const Icon=getServiceIcon(service.icon);const paragraphs=contentParagraphs(service.content,service.description);return <main><section className="page-hero" style={{position:'relative',zIndex:2}}><div className="container"><div className="eyebrow">خدمة من ShoTech</div><h1>{service.title}</h1><p>{service.description||'حل رقمي مصمم حسب احتياج مشروعك.'}</p></div></section><section className="section" style={{position:'relative',zIndex:1,clear:'both'}}><div className="container detail-layout"><div className="detail-copy"><div className="service-icon"><Icon size={28}/></div><div className="eyebrow">تفاصيل الخدمة</div><h2>حل رقمي معمول عشان يخدم هدفك.</h2><div className="service-content">{paragraphs.map((paragraph,index)=><p className={index===0?'lead':''} key={`${service.id}-${index}`}>{paragraph}</p>)}</div><div className="check-list"><div className="check"><span className="check-mark">✓</span><div><b>تنفيذ حسب احتياجك</b><span>بنشتغل على المطلوب فعلاً بدل ما نفرض قالب ثابت على مشروعك.</span></div></div><div className="check"><span className="check-mark">✓</span><div><b>قابل للتطوير</b><span>بنراعي إن الحل يكبر مع مشروعك وتقدر تطور عليه بعدين.</span></div></div><div className="check"><span className="check-mark">✓</span><div><b>تجربة واضحة</b><span>الهدف إن المنتج النهائي يكون عملي وسهل الاستخدام والإدارة.</span></div></div></div><Link href="/contact" className="btn btn-primary">اطلب الخدمة <ArrowLeft size={17}/></Link></div><div className="detail-image" style={{position:'relative',overflow:'hidden',isolation:'isolate'}}>{service.cover_image?<Image src={service.cover_image} alt={service.title} width={900} height={650} priority/>:<div style={{minHeight:440,height:'100%',display:'grid',placeItems:'center',padding:30,background:'#f4f4f4'}}><div style={{width:'min(360px,90%)',padding:'34px 28px',borderRadius:22,background:'#fff',border:'1px solid #ddd',textAlign:'center'}}><div style={{width:64,height:64,margin:'0 auto 18px',borderRadius:18,display:'grid',placeItems:'center',background:'#f5f5f5',border:'1px solid #ddd'}}><Icon size={32}/></div><strong style={{display:'block',fontSize:22,lineHeight:1.5}}>{service.title}</strong><p style={{margin:'10px 0 0',lineHeight:1.9,fontSize:13}}>{service.description||'حل رقمي مصمم حول احتياج مشروعك.'}</p></div></div>}</div></div></section><section className="section"><div className="container"><div className="cta"><div><div className="eyebrow">خلينا نبني</div><h2>جاهز نبدأ في خدمتك؟</h2><p>ابعت تفاصيل بسيطة عن مشروعك ونحدد مع بعض أفضل طريقة للتنفيذ.</p></div><Link href="/contact" className="btn btn-primary">تواصل معنا <ArrowLeft size={17}/></Link></div></div></section></main>}
