import { notFound } from 'next/navigation';
import Link from 'next/link';
import Image from 'next/image';
import { ArrowLeft, Code2, ShoppingCart, Building2, LayoutDashboard, MonitorSmartphone, Workflow, Sparkles, Globe2, Database, Smartphone, Palette, ShieldCheck, Rocket, Settings, Layers3, ChartNoAxesCombined, Check } from 'lucide-react';
import { getService } from '@/lib/site';
export const dynamic='force-dynamic';
const icons:any={Code2,ShoppingCart,Building2,LayoutDashboard,MonitorSmartphone,Workflow,Sparkles,Globe2,Database,Smartphone,Palette,ShieldCheck,Rocket,Settings,Layers3,ChartNoAxesCombined};
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await getService(slug);if(!s)notFound();const Icon=icons[s.icon]||Code2;return <main><section className="page-hero"><div className="container"><div className="eyebrow">SHOTECH SERVICE</div><h1>{s.title}</h1><p>{s.description}</p></div></section><section className="section"><div className="container detail-layout"><div className="detail-copy"><div className="service-icon"><Icon size={28}/></div><h2 style={{fontSize:'38px'}}>حل مبني حول احتياجك.</h2><p className="lead">{s.content||s.description}</p><div className="check-list"><div className="check"><span className="check-mark"><Check size={14}/></span><div><b>تجربة استخدام واضحة</b><span>الواجهة تتصمم عشان المستخدم يعرف يعمل إيه من غير شرح طويل.</span></div></div><div className="check"><span className="check-mark"><Check size={14}/></span><div><b>بنية قابلة للتطوير</b><span>نجهز الحل بحيث تقدر تضيف وتكبر بعدين.</span></div></div></div><Link href="/contact" className="btn btn-primary">اطلب الخدمة <ArrowLeft size={17}/></Link></div><div className="detail-image">{s.cover_image?<Image src={s.cover_image} alt={s.title} width={900} height={650}/>:<div className="visual-center"><div className="visual-card"><Icon size={34}/><p>حل رقمي مصمم حول احتياج مشروعك.</p></div></div>}</div></div></section><section className="container section" style={{paddingTop:0}}><div className="cta"><div><div className="eyebrow">LET'S BUILD</div><h2>جاهز نخلي الخدمة تشتغل لصالحك؟</h2><p>ابعت تفاصيل بسيطة عن مشروعك ونبدأ من هنا.</p></div><Link href="/contact" className="btn btn-primary">تواصل معنا <ArrowLeft size={17}/></Link></div></section></main>}
