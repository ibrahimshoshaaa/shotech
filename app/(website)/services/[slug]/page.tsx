import {notFound} from 'next/navigation';import Link from 'next/link';import Image from 'next/image';import {ArrowRight,Code2,ShoppingCart,Building2,LayoutDashboard,MonitorSmartphone,Workflow,Sparkles,Globe2,Database,Smartphone,Palette,ShieldCheck,Rocket,Settings,Layers3} from 'lucide-react';import {getService} from '@/lib/site';
export const dynamic='force-dynamic';
const icons:any={Code2,ShoppingCart,Building2,LayoutDashboard,MonitorSmartphone,Workflow,Sparkles,Globe2,Database,Smartphone,Palette,ShieldCheck,Rocket,Settings,Layers3};
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await getService(slug);if(!s)notFound();const Icon=icons[s.icon]||Code2;return <main className="container page service-detail"><div className="eyebrow">SERVICE</div><div className="service-detail-hero"><div><div className="service-icon"><Icon size={30}/></div><h1>{s.title}</h1><p className="lead muted">{s.description}</p></div>{s.cover_image&&<Image src={s.cover_image} alt={s.title} width={700} height={450} style={{width:'100%',height:'auto',borderRadius:24}}/>}</div><div className="project-content"><p className="muted" style={{fontSize:18,whiteSpace:'pre-wrap'}}>{s.content||s.description}</p></div><div className="cta-banner"><div><div className="eyebrow">LET’S BUILD</div><h2>Need this service?</h2><p>Tell us about your business and we will shape the right solution.</p></div><Link className="btn btn-large" href="/contact">Start a Project <ArrowRight size={18}/></Link></div></main>}
