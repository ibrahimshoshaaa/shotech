'use client';
import {useEffect,useRef,useState} from 'react';
import {Pencil,Trash2,Star,Image as ImageIcon,X,ArrowUp,ArrowDown} from 'lucide-react';
import {PageHeader,SectionCard,Field,SaveBar,StatusBadge} from '@/components/admin/AdminUI';
const blank:any={title:'',slug:'',excerpt:'',content:'',cover_image:'',images:[],technologies:[],category:'',demo_url:'',github_url:'',status:'draft',featured:false};
export default function Page(){const [items,setItems]=useState<any[]>([]),[form,setForm]=useState<any>({...blank}),[edit,setEdit]=useState<number|null>(null),[uploading,setUploading]=useState(false),[error,setError]=useState('');const ref=useRef<HTMLInputElement>(null);const load=async()=>setItems(await (await fetch('/api/projects')).json());useEffect(()=>{load()},[]);const set=(k:string,v:any)=>setForm((f:any)=>({...f,[k]:v}));const normalize=(p:any)=>({...p,images:Array.isArray(p.images)?p.images:JSON.parse(p.images||'[]'),technologies:Array.isArray(p.technologies)?p.technologies:JSON.parse(p.technologies||'[]'),featured:!!p.featured});async function upload(files:FileList|null){if(!files?.length)return;setUploading(true);setError('');try{const fd=new FormData();Array.from(files).forEach(file=>fd.append('files',file));const r=await fetch('/api/upload',{method:'POST',body:fd});const d=await r.json();if(!r.ok)throw new Error(d.error||'فشل رفع الصور');const urls=d.urls||(d.url?[d.url]:[]);setForm((f:any)=>{const images=[...(f.images||[]),...urls];return {...f,images,cover_image:f.cover_image||images[0]||''}})}catch(e:any){setError(e.message)}finally{setUploading(false);if(ref.current)ref.current.value=''}}function removeImage(i:number){setForm((f:any)=>{const images=f.images.filter((_:string,n:number)=>n!==i);return {...f,images,cover_image:f.cover_image===f.images[i]?(images[0]||''):f.cover_image}})}function move(i:number,d:-1|1){setForm((f:any)=>{const a=[...f.images],t=i+d;if(t<0||t>=a.length)return f;[a[i],a[t]]=[a[t],a[i]];return {...f,images:a}})}async function save(e:any){e.preventDefault();setError('');const payload={...form,cover_image:form.cover_image||form.images?.[0]||''};const r=await fetch(edit?`/api/projects/${edit}`:'/api/projects',{method:edit?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const d=await r.json();if(!r.ok){setError(d.error||'تعذر الحفظ');return}cancel();load()}async function del(i:number){if(!confirm('حذف المشروع؟'))return;await fetch(`/api/projects/${i}`,{method:'DELETE'});load()}function cancel(){setEdit(null);setForm({...blank});setError('')}return <><PageHeader eyebrow="المشاريع" title="أعمال ShoTech" description="أضف المشاريع، الصور، التقنيات وروابط العرض والنشر من شاشة واحدة."/><form onSubmit={save}><SectionCard title={edit?'تعديل المشروع':'إضافة مشروع'} description="ابدأ بالمعلومات الأساسية ثم أضف معرض الصور والتفاصيل."><div className="admin-grid"><Field label="اسم المشروع" required><input value={form.title} onChange={e=>set('title',e.target.value)} required placeholder="اسم المشروع"/></Field><Field label="الرابط المختصر"><input value={form.slug} onChange={e=>set('slug',e.target.value)} placeholder="project-name"/></Field></div><div className="admin-grid"><Field label="التصنيف"><input value={form.category} onChange={e=>set('category',e.target.value)} placeholder="متجر · نظام · تطبيق"/></Field><Field label="التقنيات" hint="افصل بين التقنيات بفاصلة."><input value={(form.technologies||[]).join(', ')} onChange={e=>set('technologies',e.target.value.split(',').map((x:string)=>x.trim()).filter(Boolean))} placeholder="Next.js, TypeScript"/></Field></div><Field label="وصف مختصر"><textarea value={form.excerpt} onChange={e=>set('excerpt',e.target.value)} placeholder="وصف يظهر في بطاقة المشروع..."/></Field><Field label="تفاصيل المشروع"><textarea style={{minHeight:170}} value={form.content} onChange={e=>set('content',e.target.value)} placeholder="التفاصيل الكاملة..."/></Field></SectionCard><SectionCard title="معرض الصور" description="ارفع أكثر من صورة، واختر صورة الغلاف ورتّب الصور كما تريد."><input ref={ref} type="file" accept="image/*" multiple onChange={e=>upload(e.target.files)}/>{uploading&&<div className="admin-notice" style={{marginTop:12}}>جاري رفع الصور...</div>}{!!form.images?.length&&<div className="admin-image-grid">{form.images.map((url:string,i:number)=><div className={`admin-gallery-card ${form.cover_image===url?'cover':''}`} key={`${url}-${i}`}><img src={url} alt=""/><div className="admin-gallery-actions"><button type="button" className="admin-action" onClick={()=>set('cover_image',url)}><Star size={13}/> غلاف</button><button type="button" className="admin-action" disabled={i===0} onClick={()=>move(i,-1)}><ArrowUp size={13}/></button><button type="button" className="admin-action" disabled={i===form.images.length-1} onClick={()=>move(i,1)}><ArrowDown size={13}/></button><button type="button" className="admin-action danger" onClick={()=>removeImage(i)}><X size={13}/></button></div></div>)}</div>}</SectionCard><SectionCard title="النشر والروابط"><div className="admin-grid"><Field label="حالة المشروع"><select value={form.status} onChange={e=>set('status',e.target.value)}><option value="draft">مسودة</option><option value="published">منشور</option></select></Field><Field label="رابط العرض"><input value={form.demo_url} onChange={e=>set('demo_url',e.target.value)} placeholder="https://..."/></Field><Field label="GitHub"><input value={form.github_url} onChange={e=>set('github_url',e.target.value)} placeholder="https://github.com/..."/></Field></div><label className="admin-check"><input type="checkbox" checked={!!form.featured} onChange={e=>set('featured',e.target.checked)}/> <Star size={15}/> عرض المشروع في المشاريع المميزة</label>{error&&<div className="admin-error">{error}</div>}<SaveBar editing={!!edit} onCancel={cancel} saving={uploading}/></SectionCard></form><SectionCard title="كل المشاريع" description={`${items.length} مشروع مسجل.`}>{items.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>المشروع</th><th>التصنيف</th><th>الصور</th><th>الحالة</th><th>مميز</th><th></th></tr></thead><tbody>{items.map(p=>{const images=(()=>{try{return JSON.parse(p.images||'[]')}catch{return[]}})();return <tr key={p.id}><td><strong>{p.title}</strong></td><td>{p.category||'—'}</td><td><ImageIcon size={14}/> {images.length}</td><td><StatusBadge tone={p.status==='published'?'success':'warning'}>{p.status==='published'?'منشور':'مسودة'}</StatusBadge></td><td>{p.featured?<Star size={14}/>: '—'}</td><td><div className="admin-actions"><button className="admin-action" onClick={()=>{setEdit(p.id);setForm(normalize(p));window.scrollTo({top:0,behavior:'smooth'})}}><Pencil size={14}/> تعديل</button><button className="admin-action danger" onClick={()=>del(p.id)}><Trash2 size={14}/> حذف</button></div></td></tr>})}</tbody></table></div>:<div className="admin-empty"><h3>لا توجد مشاريع</h3><p>أضف أول مشروع لعرض أعمالك.</p></div>}</SectionCard></>}
