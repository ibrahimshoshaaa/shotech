'use client';import {useEffect,useState} from 'react';const blank:any={title:'',slug:'',excerpt:'',content:'',cover_image:'',images:[],technologies:[],category:'',demo_url:'',github_url:'',status:'draft',featured:false};export default function Page(){const [items,setItems]=useState<any[]>([]),[form,setForm]=useState<any>(blank),[edit,setEdit]=useState<any>(null);const load=()=>fetch('/api/projects').then(r=>r.json()).then(setItems);useEffect(()=>{load()},[]);const set=(k:string,v:any)=>setForm((f:any)=>({...f,[k]:v}));async function save(e:any){e.preventDefault();const url=edit?`/api/projects/${edit}`:'/api/projects';await fetch(url,{method:edit?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setForm(blank);setEdit(null);load()}async function del(id:number){if(confirm('Delete this project?')){await fetch(`/api/projects/${id}`,{method:'DELETE'});load()}}async function upload(e:any){const file=e.target.files?.[0];if(!file)return;const fd=new FormData();fd.append('file',file);const r=await fetch('/api/upload',{method:'POST',body:fd});const d=await r.json();if(d.url)set('cover_image',d.url);else alert(d.error||'Upload failed')}return <><div className="row"><div><div className="eyebrow">PROJECTS</div><h1>Manage Projects</h1></div></div><form className="form card" onSubmit={save}><input className="input" value={form.title} onChange={e=>set('title',e.target.value)} placeholder="Project title" required/><input className="input" value={form.slug} onChange={e=>set('slug',e.target.value)} placeholder="Custom slug (optional)"/><input className="input" value={form.category} onChange={e=>set('category',e.target.value)} placeholder="Category"/><textarea value={form.excerpt} onChange={e=>set('excerpt',e.target.value)} placeholder="Short description"/><textarea value={form.content} onChange={e=>set('content',e.target.value)} placeholder="Full project content"/><input className="input" value={form.cover_image} onChange={e=>set('cover_image',e.target.value)} placeholder="Cover image URL"/><input className="input" type="file" accept="image/*" onChange={upload}/><input className="input" value={form.technologies.join(', ')} onChange={e=>set('technologies',e.target.value.split(',').map((x:string)=>x.trim()).filter(Boolean))} placeholder="Technologies: Next.js, TypeScript"/><input className="input" value={form.demo_url} onChange={e=>set('demo_url',e.target.value)} placeholder="Demo URL"/><input className="input" value={form.github_url} onChange={e=>set('github_url',e.target.value)} placeholder="GitHub URL"/><select value={form.status} onChange={e=>set('status',e.target.value)}><option value="draft">Draft</option><option value="published">Published</option></select><label><input type="checkbox" checked={form.featured} onChange={e=>set('featured',e.target.checked)}/> Featured on Home</label><div><button className="btn">{edit?'Update Project':'Add Project'}</button> {edit&&<button type="button" className="btn alt" onClick={()=>{setEdit(null);setForm(blank)}}>Cancel</button>}</div></form><h2>All Projects</h2><div style={{overflowX:'auto'}}><table className="table"><thead><tr><th>Title</th><th>Status</th><th>Featured</th><th>Actions</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.status}</td><td>{p.featured?'Yes':'No'}</td><td><button className="btn small alt" onClick={()=>{setEdit(p.id);setForm({...p,images:JSON.parse(p.images||'[]'),technologies:JSON.parse(p.technologies||'[]'),featured:!!p.featured})}}>Edit</button> <button className="btn small danger" onClick={()=>del(p.id)}>Delete</button></td></tr>)}</tbody></table></div></>}
