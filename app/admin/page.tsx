import {query} from '@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const [[p],[pub],[s],[m],[u]] = await Promise.all(['SELECT count(*) c FROM projects','SELECT count(*) c FROM projects WHERE status=\'published\'','SELECT count(*) c FROM services','SELECT count(*) c FROM messages','SELECT count(*) c FROM messages WHERE status=\'unread\''].map(async sql=>(await query(sql)).rows));const data=[['Projects',(p as any).c],['Published',(pub as any).c],['Services',(s as any).c],['Messages',(m as any).c],['Unread',(u as any).c]];return <><div className="eyebrow">ADMIN DASHBOARD</div><h1>Overview</h1><div className="stats">{data.map(x=><div className="stat" key={x[0]}><span className="muted">{x[0]}</span><strong>{x[1]}</strong></div>)}</div><section className="card" style={{marginTop:20}}><h2>Control center</h2><p className="muted">Everything shown on the public website is managed from this dashboard. Add services, publish projects, read client messages and update company settings without touching code.</p></section></>}
