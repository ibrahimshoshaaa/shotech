'use client';
import {useEffect,useState} from 'react';
import {Save} from 'lucide-react';
import {PageHeader,SectionCard,Field} from '@/components/admin/AdminUI';
const groups:any=[['الهوية والعلامة',['companyName','tagline']],['بيانات التواصل',['email','whatsapp','facebook','instagram','linkedin','github']],['تحسين محركات البحث SEO',['seoTitle','seoDescription']],['صفحة من نحن',['aboutTitle','aboutBody']],['طريقة العمل',['processTitle','processBody']]];
const labels:any={companyName:'اسم الشركة',tagline:'الشعار المختصر',email:'البريد الإلكتروني',whatsapp:'رقم واتساب',facebook:'Facebook',instagram:'Instagram',linkedin:'LinkedIn',github:'GitHub',seoTitle:'عنوان SEO',seoDescription:'وصف SEO',aboutTitle:'عنوان من نحن',aboutBody:'محتوى من نحن',processTitle:'عنوان طريقة العمل',processBody:'محتوى طريقة العمل'};
export default function Page(){const [f,setF]=useState<any>({}),[saving,setSaving]=useState(false),[saved,setSaved]=useState(false);useEffect(()=>{fetch('/api/settings').then(r=>r.json()).then(setF)},[]);const set=(k:string,v:string)=>{setSaved(false);setF((x:any)=>({...x,[k]:v}))};async function save(e:any){e.preventDefault();setSaving(true);const r=await fetch('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setSaving(false);if(!r.ok){alert('غير مصرح بالحفظ');return}setSaved(true)}return <><PageHeader eyebrow="إعدادات الموقع" title="الإعدادات العامة" description="بيانات الشركة والتواصل وSEO والمحتوى الافتراضي — كلها مرتبة في مجموعات واضحة."/><form onSubmit={save}>{groups.map(([title,keys]:any)=><SectionCard key={title} title={title}>{<div className="admin-grid">{keys.map((k:string)=>{const area=k.toLowerCase().includes('body')||k.toLowerCase().includes('description');return <Field key={k} label={labels[k]||k}>{area?<textarea value={f[k]||''} onChange={e=>set(k,e.target.value)}/>:<input value={f[k]||''} onChange={e=>set(k,e.target.value)} placeholder={k==='whatsapp'?'+20...':''}/>}</Field>})}</div>}</SectionCard>)}<div className="admin-savebar"><button className="admin-primary" disabled={saving}><Save size={17}/>{saving?'جاري الحفظ...':'حفظ كل الإعدادات'}</button>{saved&&<span style={{color:'#187a45',fontSize:12}}>تم الحفظ بنجاح ✓</span>}</div></form></>}
