'use client';import {useEffect,useState} from 'react';const keys=['companyName','tagline','email','whatsapp','facebook','instagram','linkedin','github','seoTitle','seoDescription'];export default function Page(){const [f,setF]=useState<any>({});useEffect(()=>{fetch('/api/settings').then(r=>r.json()).then(setF)},[]);async function save(e:any){e.preventDefault();await fetch('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});alert('Settings saved')}return <><div className="eyebrow">SETTINGS</div><h1>Site Settings</h1><form className="form card" onSubmit={save}>{keys.map(k=><label key={k}>{k}<input className="input" value={f[k]||''} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<button className="btn">Save Settings</button></form></>}
