'use client';
import{useRouter}from'next/navigation';import{useState}from'react';import{LogIn,ShieldCheck,Eye,EyeOff}from'lucide-react';
export default function Page(){const router=useRouter();const[error,setError]=useState('');const[loading,setLoading]=useState(false);const[show,setShow]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);try{const form=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});if(res.ok){router.replace('/admin');router.refresh();return}setError(res.status===429?'محاولات كثيرة. انتظر قليلًا ثم حاول مرة أخرى.':'البريد الإلكتروني أو كلمة المرور غير صحيحة.')}catch{setError('تعذر الاتصال بالخادم. حاول مرة أخرى.')}finally{setLoading(false)}}return <main className="admin-login"><div className="admin-login-card"><div className="admin-login-brand">Sho<span>Tech</span><small>لوحة التحكم</small></div><div className="login-icon"><ShieldCheck size={23}/></div><h1>مرحبًا بعودتك</h1><p>سجّل الدخول لإدارة موقع ShoTech ومحتواه.</p><form onSubmit={submit}><label className="admin-field"><span>البريد الإلكتروني</span><input name="email" type="email" autoComplete="username" placeholder="admin@example.com" required/></label><label className="admin-field" style={{marginTop:14}}><span>كلمة المرور</span><div className="login-password"><input name="password" type={show?'text':'password'} autoComplete="current-password" placeholder="••••••••" required/><button type="button" onClick={()=>setShow(v=>!v)} aria-label={show?'إخفاء كلمة المرور':'إظهار كلمة المرور'}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div className="admin-error" role="alert">{error}</div>}<button type="submit" className="admin-primary login-submit" disabled={loading}><LogIn size={17}/>{loading?'جاري الدخول...':'تسجيل الدخول'}</button><div className="login-security"><ShieldCheck size={14}/> جلسة الإدارة محمية ومخصصة للمشرف.</div></form></div></main>}
