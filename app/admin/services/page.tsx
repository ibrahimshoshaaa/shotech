'use client';import {useEffect,useState} from 'react';export default function Page(){const [x,setX]=useState<any[]>([]),[f,setF]=useState<any>({title:'',description:'',icon:'Code2',sort_order:0}),[id,setId]=useState<number|null>(null);const load=()=>fetch('/api/services').then(r=>r.json()).then(setX);useEffect(()=>{load()},[]);async function save(e:any){e.preventDefault();await fetch(id?`/api/services/${id}`:'/api/services',{method:id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setId(null);setF({title:'',description:'',icon:'Code2',sort_order:0});load()}async function del(i:number){await fetch(`/api/services/${i}`,{method:'DELETE'});load()}return <><div className="eyebrow">SERVICES</div><h1>Manage Services</h1><form className="form card" onSubmit={save}><input className="input" value={f.title} onChange={e=>setF({...f,title:e.target.value})} placeholder="Service title" required/><textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})} placeholder="Description"/><input className="input" value={f.icon} onChange={e=>setF({...f,icon:e.target.value})} placeholder="Icon name"/><input className="input" type="number" value={f.sort_order} onChange={e=>setF({...f,sort_order:+e.target.value})}/><button className="btn">{id?'Update':'Add Service'}</button></form><div className="grid" style={{marginTop:20}}>{x.map(s=><div className="card" key={s.id}><h3>{s.title}</h3><p className="muted">{s.description}</p><button className="btn small alt" onClick={()=>{setId(s.id);setF(s)}}>Edit</button> <button className="btn small danger" onClick={()=>del(s.id)}>Delete</button></div>)}</div></>}
