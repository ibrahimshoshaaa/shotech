import {v2 as cloudinary} from 'cloudinary';export async function POST(req:Request){if(!process.env.CLOUDINARY_API_KEY)return Response.json({error:'Cloudinary is not configured'},{status:501});cloudinary.config({cloud_name:process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME,api_key:process.env.CLOUDINARY_API_KEY,api_secret:process.env.CLOUDINARY_API_SECRET});const form=await req.formData(),file=form.get('file') as File;if(!file)return Response.json({error:'No file'},{status:400});const buf=Buffer.from(await file.arrayBuffer());const result=await new Promise<any>((resolve,reject)=>cloudinary.uploader.upload_stream({folder:'shotech'},(e,r)=>e?reject(e):resolve(r)).end(buf));return Response.json({url:result.secure_url})}
