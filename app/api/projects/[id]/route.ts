import {query} from '@/lib/db';import {json,slugify} from '@/lib/utils';import {isAdmin} from '@/lib/admin';
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){try{if(!await isAdmin())return Response.json({error:'Unauthorized'},{status:401});const {id}=await params,b=await req.json();await query('UPDATE projects SET title=?,slug=?,excerpt=?,content=?,cover_image=?,images=?,technologies=?,category=?,demo_url=?,github_url=?,status=?,featured=?,updated_at=CURRENT_TIMESTAMP WHERE id=?',[b.title,slugify(b.slug||b.title),b.excerpt||'',b.content||'',b.cover_image||'',json(b.images||[]),json(b.technologies||[]),b.category||'',b.demo_url||'',b.github_url||'',b.status||'draft',b.featured?1:0,id]);return Response.json({ok:true})}catch(e:any){return Response.json({error:e.message},{status:400})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return Response.json({error:'Unauthorized'},{status:401});const {id}=await params;await query('DELETE FROM projects WHERE id=?',[id]);return Response.json({ok:true})}
