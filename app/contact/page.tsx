'use client';import {useState} from 'react';export default function Page(){const [ok,setOk]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setOk(r.ok?'Message sent successfully.':'Something went wrong.');if(r.ok)e.currentTarget.reset()}return <main className="container page"><div className="eyebrow">CONTACT</div><h1>Let's build your next system.</h1><form className="form" onSubmit={submit}><input className="input" name="name" placeholder="Your name" required/><input className="input" name="email" type="email" placeholder="Email" required/><input className="input" name="phone" placeholder="Phone"/><textarea name="message" placeholder="Tell us about your project" required/><button className="btn">Send Message</button>{ok&&<p>{ok}</p>}</form></main>}
