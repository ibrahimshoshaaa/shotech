import {getServices} from '@/lib/site';export const dynamic='force-dynamic';export default async function Page(){const s=await getServices();return <main className="container page"><div className="eyebrow">SERVICES</div><h1>Built around your business.</h1><div className="grid">{s.map((x:any)=><article className="card" key={x.id}><h2>{x.title}</h2><p className="muted">{x.description}</p></article>)}</div>{!s.length&&<div className="empty">The admin has not added services yet.</div>}</main>}
