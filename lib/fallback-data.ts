/** Public fallback content is intentionally empty.
 * The admin/CMS is the single source of truth; hidden or unpublished content
 * must never be replaced with demo records.
 */
export const fallbackServices: any[] = [];
export const fallbackProjects: any[] = [];
