# Mobile-first admin

The ShoTech admin UI is designed mobile-first for phone use. The navigation becomes a fixed bottom navigation bar on small screens, forms use touch-friendly controls, dashboard stats use compact cards, and wide desktop tables remain available through contained scrolling where a table layout is required.

The admin visual language uses the dark/gold ShoTech admin palette consistently.
